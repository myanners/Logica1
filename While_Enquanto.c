#include <stdio.h>
#include <stdlib.h>

int main()
{
    int contador = 1;

    while (contador <= 200)
    {
        printf (" teste %i\n", contador);
        ++contador;
    }
    return 0;
}
